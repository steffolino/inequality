{
  "name": "db-proxy",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "tsc --noEmit",
    "seed:local": "npx wrangler d1 execute inequality-edge-db --local --file ../api-python/d1/schema.sql",
    "seed:remote": "npx wrangler d1 execute inequality-edge-db --file ../api-python/d1/schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240821.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.7"
  }
}